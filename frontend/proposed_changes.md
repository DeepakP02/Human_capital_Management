### SUPERADMIN Modules

### ADMIN Modules

#### [MODIFY] [AdminDashboard.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/admin/AdminDashboard.jsx)
- Modify `<h1>` heading "Admin Dashboard" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [OrgSetup.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/admin/OrgSetup.jsx)
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [Departments.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/admin/Departments.jsx)
- Modify `<h1>` heading "Departments" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [Users.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/admin/Users.jsx)
- Modify `<h1>` heading "User Management" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [RolesPermissions.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/admin/RolesPermissions.jsx)
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [PayrollCenter.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/admin/PayrollCenter.jsx)
- Modify `<h1>` heading "Payroll Center" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [Holidays.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/admin/Holidays.jsx)
- Modify `<h1>` heading "Holiday Management" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [BenefitsConfig.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/admin/BenefitsConfig.jsx)
- Modify `<h1>` heading "Benefits Configuration" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [AICenter.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/admin/AICenter.jsx)
- Modify `<h1>` heading "AI Center" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [ComplianceCenter.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/admin/ComplianceCenter.jsx)
- Modify `<h1>` heading "Compliance Center" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [Integrations.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/admin/Integrations.jsx)
- Modify `<h1>` heading "System Integrations" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [Billing.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/admin/Billing.jsx)
- Modify `<h1>` heading "Billing & Subscriptions" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [AuditLogs.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/admin/AuditLogs.jsx)
- Modify `<h1>` heading "System Audit Logs" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [AdminReports.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/admin/AdminReports.jsx)
- Modify `<h1>` heading "Organization Reports" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [Settings.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/admin/Settings.jsx)
- Modify `<h1>` heading "System Settings" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [AdminProfile.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/admin/AdminProfile.jsx)
- Modify `<h1>` heading "My Profile" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

### MANAGER Modules

#### [MODIFY] [ManagerDashboard.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/manager/ManagerDashboard.jsx)
- Modify `<h1>` heading "Manager Dashboard" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [TeamMembers.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/manager/TeamMembers.jsx)
- Modify `<h1>` heading "Team Members" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [AttendanceReview.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/manager/AttendanceReview.jsx)
- Modify `<h1>` heading "Attendance Review" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [LeaveApproval.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/manager/LeaveApproval.jsx)
- Modify `<h1>` heading "Leave Approval" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [KPITracking.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/manager/KPITracking.jsx)
- Modify `<h1>` heading "KPI Tracking & Goals" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [Tasks.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/manager/Tasks.jsx)
- Modify `<h1>` heading "Team Task Management" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [Reviews.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/manager/Reviews.jsx)
- Modify `<h1>` heading "Performance Reviews" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [Reports.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/manager/Reports.jsx)
- Modify `<h1>` heading "Team Insights & Reports" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [ManagerProfile.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/manager/ManagerProfile.jsx)
- Modify `<h1>` heading "Manager Profile" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [ManagerSettings.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/manager/ManagerSettings.jsx)
- Modify `<h1>` heading "Manager Settings" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

### HR Modules

#### [MODIFY] [HRDashboard.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/hr/HRDashboard.jsx)
- Modify `<h1>` heading "HR Dashboard" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [JobPosts.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/hr/JobPosts.jsx)
- Modify `<h1>` heading "Job Posts" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [Candidates.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/hr/Candidates.jsx)
- Modify `<h1>` heading "Candidates" to include `dark:text-white` text color style.
- Modify `<h1>` heading "{previewingResume.name}" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [InterviewManagement.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/hr/InterviewManagement.jsx)
- Modify `<h1>` heading "Interview Scheduling" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [HiringPipeline.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/hr/HiringPipeline.jsx)
- Modify `<h1>` heading "Hiring Pipeline" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [OfferManagement.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/hr/OfferManagement.jsx)
- Modify `<h1>` heading "Offer Management" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [Onboarding.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/hr/Onboarding.jsx)
- Modify `<h1>` heading "Onboarding" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [Reports.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/hr/Reports.jsx)
- Modify `<h1>` heading "Reports & Analytics" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [Messages.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/hr/Messages.jsx)
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [HRProfile.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/hr/HRProfile.jsx)
- Modify `<h1>` heading "HR Profile" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [HRSettings.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/hr/HRSettings.jsx)
- Modify `<h1>` heading "Profile Settings" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

### EMPLOYEE Modules

#### [MODIFY] [EmployeeDashboard.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/employee/EmployeeDashboard.jsx)
- Modify `<h1>` heading "Welcome Back, {profile.fullName.split(' ')[0]}!" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [EmployeeProfile.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/employee/EmployeeProfile.jsx)
- Modify `<h1>` heading "My Profile" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [EmployeeAttendance.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/employee/EmployeeAttendance.jsx)
- Modify `<h1>` heading "Time & Attendance" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [EmployeeLeave.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/employee/EmployeeLeave.jsx)
- Modify `<h1>` heading "Time Off Management" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [EmployeePayroll.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/employee/EmployeePayroll.jsx)
- Modify `<h1>` heading "Financial Hub" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [EmployeeBenefits.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/employee/EmployeeBenefits.jsx)
- Modify `<h1>` heading "Benefits & Wellness" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [EmployeeDocuments.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/employee/EmployeeDocuments.jsx)
- Modify `<h1>` heading "Records & Vault" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [EmployeePerformance.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/employee/EmployeePerformance.jsx)
- Modify `<h1>` heading "Strategy & Performance" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [EmployeeHelpDesk.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/employee/EmployeeHelpDesk.jsx)
- Modify `<h1>` heading "Support Ecosystem" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [EmployeeSettings.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/employee/EmployeeSettings.jsx)
- Modify `<h1>` heading "Employee Settings" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

### CANDIDATE Modules

#### [MODIFY] [CandidateDashboard.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/candidate/CandidateDashboard.jsx)
- Modify `<h1>` heading "Candidate Dashboard" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [BrowseJobs.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/candidate/BrowseJobs.jsx)
- Modify `<h1>` heading "BROWSE JOBS" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [ApplicationForm.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/candidate/ApplicationForm.jsx)
- Modify `<h1>` heading "ENGAGEMENT PROTOCOL" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [MyApplications.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/candidate/MyApplications.jsx)
- Modify `<h1>` heading "My Applications" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [ResumeBuilder.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/candidate/ResumeBuilder.jsx)
- Modify `<h1>` heading "Resume Builder" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [AIResumeScore.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/candidate/AIResumeScore.jsx)
- Modify `<h1>` heading "AI Resume Score" to include `dark:text-white` text color style.
- Modify `<h1>` heading "Resume Analysis Results" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [InterviewSchedule.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/candidate/InterviewSchedule.jsx)
- Modify `<h1>` heading "UPCOMING INTERVIEWS" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [Notifications.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/candidate/Notifications.jsx)
- Modify `<h1>` heading "SIGNAL QUEUE" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [CandidateProfile.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/candidate/CandidateProfile.jsx)
- Modify `<h1>` heading "{formData.fullName}" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [CandidateSettings.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/candidate/CandidateSettings.jsx)
- Modify `<h1>` heading "SETTINGS" to include `dark:text-white` text color style.
- Modify `<h1>` heading "SETTINGS" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

### OTHER Modules

#### [MODIFY] [LoginPage.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/auth/LoginPage.jsx)
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [LandingPage.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/LandingPage.jsx)
- Modify `<h1>` heading "Transform Workforce <br /> <span className="text-transparent bg-clip-text bg-gradient-to-r from-primary-600 to-indigo-600">Management</span> with AI" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

#### [MODIFY] [BookDemo.jsx](file:///c:/Users/abc/Human_capital_Management/frontend/src/features/BookDemo.jsx)
- Modify `<h1>` heading "Transform Your Workforce with <span className="text-transparent bg-clip-text bg-gradient-to-r from-primary-600 to-indigo-600">HCM.ai</span>" to include `dark:text-white` text color style.
- Standardize inline backgrounds (`bg-white` -> `bg-white dark:bg-slate-900`, `bg-slate-50` -> `bg-slate-50 dark:bg-slate-950`) to support dark mode.

